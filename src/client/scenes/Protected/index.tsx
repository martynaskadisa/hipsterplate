import * as React from 'react';
import { RouteComponentProps } from 'react-router-dom';

const Private: React.SFC<RouteComponentProps<{}>> = () => (
  <div>Private route</div>
);

export default Private;
